import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'kaiju-calculator',
  webDir: 'dist/kaiju-calculator',
  bundledWebRuntime: false
};

export default config;
